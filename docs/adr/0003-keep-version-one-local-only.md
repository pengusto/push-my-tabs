# Keep version one local-only

Version one uses no accounts, servers, analytics, advertising, or telemetry and does not read URLs, page titles, browsing history, or page content. It stores only extension settings through Chrome storage, keeping permissions narrow and the Chrome Web Store privacy disclosure simple.
